# Legacy / superseded artifacts

These files are **historical** — kept for context, not current. They describe an
earlier "BuildFlow" SQLite prototype and the original product vision, both
superseded by the rebuilt Postgres app.

For the current app see the root [`README.md`](../../README.md),
[`CHANGELOG.md`](../../CHANGELOG.md), and [`../launch-readiness.md`](../launch-readiness.md).
The live schema is `src/db/schema.ts` (not the SQL files here).

- `old-launch-readiness.md` — runbook for the SQLite/BuildFlow prototype.
- `old-product-blueprint.md` — original multi-module BuildFlow vision (Field,
  Production, Labor, etc. — broader than the shipped v1).
- `old-supabase-schema.sql.txt`, `old-domain.ts.txt` — early planning schema/types.
